pragma solidity ^0.5.0;

contract ExternalContract7 {

    function isValidSignature(
        bytes calldata _data,
        bytes calldata _signature)
        external
        returns (uint256 x)
    {
        x = 0;
    }
}
